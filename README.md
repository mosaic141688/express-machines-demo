# express-machines-demo

Demo for node machine controller actions

Actions should be ```find```, ```findOne```, ```create```, etc... inside the controller folder

the existing ccontroller is user, so its found on the ```/user``` endpoint
